months = [
    None,
    "January",
    "February",
    "March",
    "April",
    "May",
    "June",
    "July",
    "August",
    "September",
    "October",
    "November",
    "December"
]

def main():


def get_date(prompt):
    while True:
        try:
            item = input(prompt)
            list[item] = list.get(item, 0) + 1
        except EOFError:
            return list

def is_valid_date(date):
    if date.find("/") >= 0:
        m, d, y = date.split("/")
    else:
        m
    pass


main()